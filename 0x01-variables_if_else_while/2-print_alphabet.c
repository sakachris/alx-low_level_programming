#include <stdio.h>
/* printing alphabets in lower case */

/**
 * main - Entry point
 *
 * Return: 0 if success
 */
int main(void)
{
	char c;

	/* putchar alphabets from a to z */
	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	return (0);
}
