#include <stdio.h>

/**
 * main - entry point
 * @argc: argument counter
 * @argv: pointer to arguments passed
 *
 * Return: 0 if success
 */

int main(int argc, char *argv[])
{

	(void)argv;

	printf("%d\n", argc - 1);
	return (0);
}
