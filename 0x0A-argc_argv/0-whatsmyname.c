#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: pointer to arguments passed
 *
 * Return: 0 if success
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
